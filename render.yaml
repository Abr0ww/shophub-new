services:
  - type: web
    name: shophub
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Note: PORT is automatically provided by Render.com - do not set it manually

